# AI Agent 工作规则

本文档定义了AI助手在本项目中的工作规则,每次执行任务前必须遵守。

---

## 📋 核心规则

### 1. 文档生成规则
**❌ 没有明确要求不要主动生成任何文档**

- 不要主动创建README、说明文档、总结文档等
- 不要主动创建项目报告、分析报告等
- 只有在用户明确要求时才创建文档
- 代码注释除外,可以正常添加

**示例**:
- ❌ 错误: "我已经为你创建了一个使用说明文档..."
- ✅ 正确: 完成功能后询问 "是否需要我创建使用说明文档?"

---

### 2. 沟通语言规则
**✅ 使用中文沟通**

- 所有回复使用中文
- 代码注释使用中文
- 变量名、函数名使用英文(遵循编程规范)
- 文档内容使用中文

---

### 3. Git操作规则
**❌ 没有明确要求不要主动同步git**

- 不要主动执行 `git add`
- 不要主动执行 `git commit`
- 不要主动执行 `git push`
- 不要主动执行 `git pull`
- 只有在用户明确要求时才执行git操作

**示例**:
- ❌ 错误: "我已经将更改提交到git..."
- ✅ 正确: 完成修改后询问 "是否需要我提交这些更改到git?"

---

### 4. 测试执行规则
**❌ 没有明确要求不要主动做测试**

- 不要主动运行 `pytest`、Playwright、接口回归、页面手工验证等测试
- 不要主动进行“我先自测一下”的流程
- 只有在用户明确要求时才执行测试

**示例**:
- ❌ 错误: "我先跑一遍测试再给你结果..."
- ✅ 正确: 完成修改后询问 "是否需要我现在执行测试?"

---

## 🎯 工作流程

### 标准工作流程:
1. **理解需求** - 确认用户要求
2. **执行任务** - 完成代码修改/功能开发
3. **按需验证** - 仅在用户明确要求时执行测试
4. **询问后续** - 询问是否需要文档或git操作

### 禁止的工作流程:
1. ❌ 完成任务 → 自动生成文档 → 自动提交git
2. ❌ 完成任务 → 自动创建说明文档
3. ❌ 完成任务 → 自动推送到远程仓库

---

## ✅ 允许的主动操作

以下操作可以主动执行,无需询问:

1. **代码修改** - 根据需求修改代码
2. **代码注释** - 添加必要的代码注释
3. **错误修复** - 修复发现的bug
4. **依赖安装** - 安装必要的依赖包(询问后)

---

## ❌ 禁止的主动操作

以下操作必须经过用户明确同意:

1. **文档创建** - 任何形式的文档文件
2. **Git操作** - 任何git命令
3. **数据库迁移** - 数据库结构变更
4. **生产部署** - 部署到生产环境
5. **文件删除** - 删除现有文件(清理除外)
6. **测试执行** - 未经明确要求运行任何测试

---

## 📝 示例对话

### ✅ 正确示例:

**用户**: 添加一个新功能  
**AI**: 好的,我来添加这个功能...  
*(完成后)*  
**AI**: 功能已完成。是否需要我:
1. 现在执行测试验证?
2. 创建功能说明文档?
3. 提交更改到git?

---

### ❌ 错误示例:

**用户**: 添加一个新功能  
**AI**: 好的,我来添加这个功能...  
*(完成后)*  
**AI**: 功能已完成,我已经创建了说明文档并提交到git仓库。

---

## 🔄 规则更新

本规则文档可能会根据项目需要进行更新。每次执行任务前,AI助手应:

1. 检查本文档是否存在
2. 遵守文档中的所有规则
3. 如有疑问,优先询问用户

---

**最后更新**: 2026-02-10  
**版本**: 1.1
