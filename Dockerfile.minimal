# 最简化版本 - 使用预装Flask的镜像
FROM tiangolo/uwsgi-nginx-flask:python3.11

# 设置工作目录
WORKDIR /app

# 设置环境变量
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    FLASK_APP=app.py

# 复制项目文件
COPY . .

# 创建必要的目录
RUN mkdir -p instance logs

# 暴露端口
EXPOSE 80

# 启动命令（使用内置的uwsgi-nginx）
CMD ["/start.sh"]
