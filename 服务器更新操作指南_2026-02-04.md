# 服务器更新操作指南

**更新日期**: 2026-02-04  
**更新内容**: 课题任务管理和头脑风暴管理功能  
**Git 提交**: fadbd3d (添加数据库迁移脚本)

---

## 📋 更新概述

### 主要变更
1. ✨ 新增课题任务管理系统(班主任指派任务给老师)
2. ✨ 新增头脑风暴管理功能(记录结论和课题选项)
3. 📊 新增 2 个数据库表: `topic_tasks`, `topic_submissions`
4. 📊 为 `leads` 表新增 6 个字段
5. 🧪 新增 Playwright 测试框架
6. 📚 新增业务流程文档

### 影响范围
- **班主任**: 可创建课题任务、管理头脑风暴、访问线索列表
- **辅导老师**: 可查看和提交课题任务
- **销售**: 线索列表增加头脑风暴天数显示

---

## 🚀 服务器更新步骤

### ⚠️ 重要提示
- 本次更新包含数据库结构变更,需要执行迁移脚本
- 建议在**非高峰时段**执行更新
- 整个过程预计需要 **5-10 分钟**
- 更新期间服务会短暂中断

---

## 📝 详细操作步骤

### 步骤 1: SSH 登录服务器

```bash
# 使用您的 SSH 密钥登录
ssh -i login.pem user@your-server-ip

# 或者使用密码登录
ssh user@your-server-ip
```

---

### 步骤 2: 进入项目目录

```bash
# 进入 CRM 项目目录(根据实际路径调整)
cd /path/to/crm

# 示例路径(请根据实际情况修改):
# cd /home/ubuntu/crm
# cd /var/www/crm
# cd ~/crm
```

---

### 步骤 3: 备份当前数据库 (重要!)

```bash
# 创建备份目录
mkdir -p backups

# 备份数据库文件
cp instance/edu_crm.db backups/edu_crm_before_topic_tasks_$(date +%Y%m%d_%H%M%S).db

# 验证备份成功
ls -lh backups/edu_crm_before_topic_tasks_*

# 应该看到类似输出:
# -rw-r--r-- 1 user user 2.5M Feb  4 10:30 backups/edu_crm_before_topic_tasks_20260204_103000.db
```

---

### 步骤 4: 拉取最新代码

```bash
# 查看当前分支
git branch

# 确保在 bak 分支
git checkout bak

# 拉取最新代码
git pull origin bak

# 应该看到类似输出:
# remote: Enumerating objects: 45, done.
# remote: Counting objects: 100% (45/45), done.
# ...
# Updating 37f1acf..fadbd3d
# Fast-forward
#  14 files changed, 2295 insertions(+), 115 deletions(-)
#  create mode 100644 templates/teacher/topic_tasks.html
#  create mode 100644 migrate_topic_tasks.py
#  ...
```

---

### 步骤 5: 执行数据库迁移

```bash
# 确保迁移脚本有执行权限
chmod +x migrate_topic_tasks.py

# 执行迁移脚本
python3 migrate_topic_tasks.py

# 预期输出:
# ============================================================
# 🚀 开始执行数据库迁移: 课题任务管理功能
# ============================================================
# 📦 正在备份数据库...
# ✅ 数据库已备份到: backups/edu_crm_before_topic_tasks_20260204_103045.db
# 
# ============================================================
# 📋 开始执行迁移步骤
# ============================================================
# 📝 正在创建 topic_tasks 表...
# ✅ topic_tasks 表创建成功
# 📝 正在创建 topic_submissions 表...
# ✅ topic_submissions 表创建成功
# 📝 正在添加 leads.deposit_paid_at 字段...
# ✅ leads.deposit_paid_at 字段添加成功 - 定金支付时间(兼容字段)
# ...
# 
# ✅ 所有变更已提交到数据库
# 
# 🔍 正在验证迁移结果...
# ✅ topic_tasks 表存在, 当前记录数: 0
# ✅ topic_submissions 表存在, 当前记录数: 0
# ✅ leads.brainstorm_conclusion 字段存在
# ...
# 
# ============================================================
# 🎉 数据库迁移成功完成!
# ============================================================
# 
# 📊 迁移统计:
#   - 创建表: topic_tasks, topic_submissions
#   - 新增字段: 6 个
#   - 备份文件: backups/edu_crm_before_topic_tasks_20260204_103045.db
# 
# ✅ 可以安全地重启应用服务
```

**⚠️ 如果迁移失败**:
- 检查错误信息
- 数据库会自动回滚,不会损坏数据
- 可以从备份恢复: `cp backups/edu_crm_before_topic_tasks_*.db instance/edu_crm.db`

---

### 步骤 6: 安装新的依赖包 (如果有)

```bash
# 激活虚拟环境(如果使用)
source venv/bin/activate

# 或者
source env/bin/activate

# 安装/更新依赖
pip3 install -r requirements.txt

# 如果没有虚拟环境,直接执行:
# pip3 install -r requirements.txt --user
```

---

### 步骤 7: 重启应用服务

```bash
# 方式 1: 使用 systemd (推荐)
sudo systemctl restart crm

# 方式 2: 使用 supervisor
sudo supervisorctl restart crm

# 方式 3: 手动重启(如果使用 screen 或 tmux)
# 先找到进程并杀死
ps aux | grep "python.*run.py"
kill <PID>
# 然后重新启动
nohup python3 run.py &
```

---

### 步骤 8: 验证服务状态

```bash
# 检查服务状态
sudo systemctl status crm

# 应该看到:
# ● crm.service - CRM Application
#    Loaded: loaded (/etc/systemd/system/crm.service; enabled)
#    Active: active (running) since ...
#    ...

# 查看最新日志
sudo journalctl -u crm -n 50 -f

# 或者查看应用日志文件
tail -f logs/app.log

# 按 Ctrl+C 退出日志查看
```

---

### 步骤 9: 功能测试

#### 9.1 测试班主任功能
1. 使用班主任账号登录
2. 访问线索列表 - 应该能看到线索列表页面
3. 查看线索详情 - 应该能看到头脑风暴相关字段
4. (可选) 创建课题任务测试

#### 9.2 测试辅导老师功能
1. 使用辅导老师账号登录
2. 访问 `/teacher/topic-tasks` - 应该能看到课题任务列表页面
3. 查看任务详情

#### 9.3 检查数据库
```bash
# 进入数据库
sqlite3 instance/edu_crm.db

# 检查新表
.tables
# 应该能看到: topic_tasks, topic_submissions

# 检查表结构
.schema topic_tasks
.schema topic_submissions

# 检查 leads 表新字段
PRAGMA table_info(leads);
# 应该能看到: brainstorm_conclusion, brainstorm_topics 等字段

# 退出数据库
.quit
```

---

## ✅ 更新完成检查清单

- [ ] SSH 登录服务器成功
- [ ] 备份数据库成功
- [ ] 拉取最新代码成功
- [ ] 数据库迁移成功
- [ ] 安装依赖包成功
- [ ] 重启服务成功
- [ ] 服务状态正常
- [ ] 班主任功能测试通过
- [ ] 辅导老师功能测试通过
- [ ] 无错误日志

---

## 🆘 故障排查

### 问题 1: 数据库迁移失败

**症状**: 迁移脚本报错

**解决方案**:
```bash
# 1. 查看详细错误信息
python3 migrate_topic_tasks.py

# 2. 检查数据库文件权限
ls -l instance/edu_crm.db

# 3. 如果表已存在,迁移会自动跳过
# 4. 如果需要重新迁移,先恢复备份
cp backups/edu_crm_before_topic_tasks_*.db instance/edu_crm.db
```

### 问题 2: 服务启动失败

**症状**: `systemctl status crm` 显示 failed

**解决方案**:
```bash
# 查看详细错误日志
sudo journalctl -u crm -n 100 --no-pager

# 常见原因:
# 1. 端口被占用
sudo lsof -i :5002

# 2. Python 依赖缺失
pip3 install -r requirements.txt

# 3. 数据库文件权限问题
sudo chown www-data:www-data instance/edu_crm.db
```

### 问题 3: 页面访问 404

**症状**: 访问新功能页面返回 404

**解决方案**:
```bash
# 1. 确认代码已更新
git log --oneline -3
# 应该看到: fadbd3d 添加数据库迁移脚本

# 2. 确认服务已重启
sudo systemctl status crm

# 3. 清除浏览器缓存
# Ctrl+Shift+R (Chrome/Firefox)
```

### 问题 4: 数据库字段不存在错误

**症状**: 日志中出现 "no such column: brainstorm_conclusion"

**解决方案**:
```bash
# 1. 检查迁移是否成功执行
sqlite3 instance/edu_crm.db "PRAGMA table_info(leads);" | grep brainstorm

# 2. 如果字段不存在,重新执行迁移
python3 migrate_topic_tasks.py

# 3. 重启服务
sudo systemctl restart crm
```

---

## 📞 紧急回滚步骤

如果更新后出现严重问题,可以快速回滚:

```bash
# 1. 停止服务
sudo systemctl stop crm

# 2. 恢复数据库备份
cp backups/edu_crm_before_topic_tasks_*.db instance/edu_crm.db

# 3. 回滚代码到上一个版本
git reset --hard 37f1acf

# 4. 重启服务
sudo systemctl start crm

# 5. 验证服务状态
sudo systemctl status crm
```

---

## 📊 更新后的系统状态

### 数据库表 (总计 22 个)
- ✅ users (20 条记录)
- ✅ teachers (13 条记录)
- ✅ customers (106 条记录)
- ✅ leads (147 条记录)
- ✨ **topic_tasks** (新增)
- ✨ **topic_submissions** (新增)
- ... 其他表

### 新增功能路由
- `GET /teacher/topic-tasks` - 老师查看任务列表
- `POST /teacher/topic-tasks/<id>/submit` - 提交课题选项
- `GET/POST /leads/<id>/brainstorm` - 管理头脑风暴内容

---

## 📝 更新记录

| 日期 | 提交 | 说明 |
|------|------|------|
| 2026-02-04 | fadbd3d | 添加数据库迁移脚本 |
| 2026-02-04 | 729e352 | 新增课题任务管理和头脑风暴管理 |
| 2026-02-04 | 37f1acf | 功能增强: 完善学生详情和课程进度管理 |

---

**更新完成后请在此确认**: ✅ / ❌

**更新人**: ___________  
**更新时间**: ___________  
**备注**: ___________


