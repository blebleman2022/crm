# 班主任和辅导老师业务全流程梳理

**生成时间**: 2026-02-04  
**目的**: 全面梳理班主任(teacher_supervisor)和辅导老师(teacher)的业务流程、数据关系和权限控制

---

## 📊 一、角色体系概览

### 1.1 用户角色定义

| 角色代码 | 角色名称 | 英文名称 | 主要职责 |
|---------|---------|---------|---------|
| `admin` | 管理员 | Administrator | 系统管理、创建班主任账号 |
| `sales_manager` | 销售管理 | Sales Manager | 管理销售团队、查看所有数据 |
| `salesperson` | 销售员 | Salesperson | 创建线索、跟进客户 |
| `teacher_supervisor` | 班主任 | Class Supervisor | 管理客户交付、创建和管理辅导老师 |
| `teacher` | 辅导老师 | Tutor | 实际授课、管理学生进度 |

### 1.2 角色层级关系

```
管理员 (admin)
  └─ 创建 → 班主任 (teacher_supervisor)
              └─ 创建 → 辅导老师 (teacher)
                          └─ 负责 → 学生 (Customer)

销售员 (salesperson)
  └─ 创建 → 线索 (Lead)
              └─ 转化 → 客户 (Customer)
                          └─ 分配给 → 班主任 (teacher_supervisor)
```

---

## 📋 二、数据库表结构

### 2.1 核心表关系

#### **User 表** - 用户账号表
```sql
id                    INTEGER PRIMARY KEY
username              VARCHAR(50)
phone                 VARCHAR(11) UNIQUE
role                  VARCHAR(20)  -- admin/sales_manager/salesperson/teacher_supervisor/teacher
created_by_user_id    INTEGER      -- 辅导老师的创建人(班主任ID)
status                BOOLEAN      -- 账号状态
```

#### **Teacher 表** - 辅导老师专业信息表
```sql
user_id               INTEGER PRIMARY KEY  -- 与User.id一致
current_institution   VARCHAR(200)         -- 现单位
major_direction       VARCHAR(200)         -- 专业方向
highest_degree        VARCHAR(50)          -- 最高学历
email                 VARCHAR(100)
subject               VARCHAR(50)          -- 擅长学科
status                BOOLEAN              -- 启用状态
```

**关键关系**: `Teacher.user_id` = `User.id` (一对一)

#### **Customer 表** - 客户表
```sql
id                    INTEGER PRIMARY KEY
lead_id               INTEGER              -- 关联线索
teacher_user_id       INTEGER              -- 班主任ID (User.id, role=teacher_supervisor)
teacher_id            INTEGER              -- 辅导老师ID (Teacher.user_id)
created_at            DATETIME
updated_at            DATETIME
```

**关键关系**:
- `Customer.teacher_user_id` → `User.id` (班主任)
- `Customer.teacher_id` → `Teacher.user_id` (辅导老师)

---

## 🔄 三、完整业务流程

### 3.1 账号创建流程

#### **步骤1: 管理员创建班主任**
- **操作人**: 管理员 (admin)
- **路由**: `/admin/users/add`
- **代码**: `routes/admin.py` - `add_user()`
- **数据变更**:
  ```python
  User(
      username='张老师',
      phone='13800138000',
      role='teacher_supervisor',
      status=True
  )
  ```

#### **步骤2: 班主任创建辅导老师**
- **操作人**: 班主任 (teacher_supervisor)
- **路由**: `/teachers/add` 或 `/delivery/teachers/create`
- **代码**: 
  - `routes/teachers.py` - `add_teacher()`
  - `routes/delivery.py` - `create_teacher()`
- **数据变更**:
  ```python
  # 1. 创建User记录
  User(
      username='王老师',
      phone='13900139000',
      role='teacher',
      created_by_user_id=current_user.id,  # 班主任ID
      status=True
  )
  
  # 2. 创建Teacher记录
  Teacher(
      user_id=user.id,
      email='wang@example.com',
      subject='物理',
      major_direction='凝聚态物理'
  )
  ```

**数据隔离**: 班主任只能看到和管理自己创建的辅导老师 (`created_by_user_id == current_user.id`)

---

### 3.2 客户分配流程

#### **步骤1: 销售创建线索**
- **操作人**: 销售员 (salesperson)
- **路由**: `/leads/add`
- **数据变更**:
  ```python
  Lead(
      student_name='李明',
      sales_user_id=current_user.id,
      stage='获取联系方式'
  )
  ```

#### **步骤2: 线索转客户并分配班主任**
- **操作人**: 销售员/销售管理
- **路由**: `/leads/<lead_id>/convert`
- **代码**: `routes/leads.py` - `convert_to_customer()`
- **数据变更**:
  ```python
  Customer(
      lead_id=lead.id,
      teacher_user_id=selected_supervisor_id,  # 班主任ID
      teacher_id=None,  # 暂未分配辅导老师
      payment_amount=10000
  )
  ```

#### **步骤3: 班主任分配辅导老师**
- **操作人**: 班主任 (teacher_supervisor)
- **路由**: `/teachers/assign/<customer_id>` (POST)
- **代码**: `routes/teachers.py` - `assign_teacher()`
- **权限检查**:
  ```python
  # 1. 只能分配自己创建的辅导老师
  if user.created_by_user_id != current_user.id:
      return error('您只能分配自己创建的辅导老师')
  
  # 2. 老师必须已启用
  if not user.status:
      return error('该老师已被禁用')
  ```
- **数据变更**:
  ```python
  customer.teacher_id = teacher_user_id  # Teacher.user_id
  customer.updated_at = datetime.utcnow()
  ```

**⚠️ 注意**: 目前系统**没有记录**老师分配时间,只有 `updated_at` 字段

---

## 🎯 四、核心功能模块

### 4.1 班主任功能 (teacher_supervisor)

#### **4.1.1 管理辅导老师**

**老师列表** (`/teachers/list`)
- **权限**: 只看自己创建的老师
- **代码**: `routes/teachers.py` - `list_teachers()`
- **查询逻辑**:
  ```python
  users = User.query.filter(
      User.role == 'teacher',
      User.created_by_user_id == current_user.id
  ).all()
  
  teachers = Teacher.query.filter(
      Teacher.user_id.in_([u.id for u in users])
  ).all()
  ```

**添加老师** (`/teachers/add`)
- **代码**: `routes/teachers.py` - `add_teacher()`
- **创建**: User + Teacher 两条记录

**编辑老师** (`/teachers/edit/<teacher_id>`)
- **权限**: 只能编辑自己创建的老师
- **代码**: `routes/teachers.py` - `edit_teacher()`

**禁用/启用老师** (`/teachers/toggle_status/<teacher_id>`)
- **代码**: `routes/teachers.py` - `toggle_teacher_status()`
- **影响**: 禁用后无法被分配给新学生

#### **4.1.2 管理客户**

**客户列表** (`/customers/list`)
- **权限**: 只看自己负责的客户
- **代码**: `routes/customers.py` - `list_customers()`
- **查询逻辑**:
  ```python
  query = Customer.query.filter(
      Customer.teacher_user_id == current_user.id
  )
  ```

**分配辅导老师** (`/teachers/assign/<customer_id>`)
- **代码**: `routes/teachers.py` - `assign_teacher()`
- **下拉列表API**: `/teachers/get_active_teachers`
- **数据隔离**: 只能选择自己创建的启用老师

**更换辅导老师** (`/teachers/change/<customer_id>`)
- **代码**: `routes/teachers.py` - `change_teacher()`
- **权限**: 只能更换为自己创建的老师

**编辑客户信息** (`/customers/edit/<customer_id>`)
- **代码**: `routes/customers.py` - `edit_customer()`

#### **4.1.3 查看交付进度**

**交付管理** (`/delivery/...`)
- **课程进度**: TutoringDelivery 表
- **赛事进展**: CustomerCompetition 表
- **交付文档**: DeliveryDocument 表

---

### 4.2 辅导老师功能 (teacher)

#### **4.2.1 学生管理**

**学生列表** (`/teacher/students`)
- **权限**: 只看分配给自己的学生
- **代码**: `routes/teacher.py` - `student_list()`
- **查询逻辑**:
  ```python
  teacher = current_user.teacher_profile
  students = Customer.query.filter(
      Customer.teacher_id == teacher.user_id
  ).all()
  ```

**学生详情** (`/teacher/students/<customer_id>`)
- **代码**: `routes/teacher.py` - `student_detail()`
- **权限检查**:
  ```python
  if student.teacher_id != teacher.user_id:
      return error('您无权查看此学生信息')
  ```

#### **4.2.2 课程进度管理**

**更新课程进度** (API)
- **路由**: `/customers/<customer_id>/update_progress` (POST)
- **代码**: `routes/customers.py` - `update_customer_progress()`
- **权限**: 只能更新自己负责的学生
- **数据变更**:
  ```python
  delivery = TutoringDelivery.query.filter_by(
      customer_id=customer_id
  ).first()

  delivery.completed_sessions = new_value
  delivery.remaining_sessions = total - completed
  delivery.thesis_status = new_status
  ```

#### **4.2.3 赛事进展管理**

**添加赛事** (`/customers/<customer_id>/add_competition`)
- **代码**: `routes/customers.py` - `add_customer_competition()`
- **权限检查**:
  ```python
  if customer.teacher_id != current_user.teacher_profile.user_id:
      return error('无权限')
  ```

**更新赛事状态** (`/customers/competitions/<competition_id>/update`)
- **代码**: `routes/customers.py` - `update_competition_status()`

**删除赛事** (`/customers/competitions/<competition_id>/delete`)
- **代码**: `routes/customers.py` - `delete_competition()`

#### **4.2.4 文档上传**

**上传交付文档** (`/delivery/documents/upload`)
- **代码**: `routes/delivery.py` - `upload_document()`
- **文档类型**:
  - `thesis_draft` - 课题初稿
  - `thesis_final` - 课题终稿
  - `presentation` - 答辩PPT
  - `novelty_report` - 查新报告
  - `plagiarism_report` - 查重报告
  - `evaluation_material` - 评审材料
  - `other_materials` - 其他材料
- **数据记录**:
  ```python
  DeliveryDocument(
      customer_id=customer_id,
      doc_type=doc_type,
      file_path=file_path,
      uploaded_by_type='teacher',
      uploaded_by_id=teacher.user_id,
      uploaded_by_name=teacher.user.username,
      version=version_number,
      is_latest=True
  )
  ```

---

### 4.3 销售管理功能 (sales_manager)

#### **4.3.1 查看辅导老师**

**老师列表** (`/teachers/list_for_sales`)
- **权限**: 只读,查看所有辅导老师
- **代码**: `routes/teachers.py` - `list_teachers_for_sales()`
- **功能**: 查看老师信息、负责学生数统计

**老师详情** (`/teachers/detail_for_sales/<teacher_id>`)
- **代码**: `routes/teachers.py` - `detail_teacher_for_sales()`
- **权限**: 只读,不能编辑

---

## 🔐 五、权限控制体系

### 5.1 装饰器权限控制

#### **班主任权限装饰器**
```python
@teacher_supervisor_required
def some_function():
    # 只有 role='teacher_supervisor' 可以访问
    pass
```

**定义位置**: `routes/teachers.py` - `teacher_supervisor_required()`

#### **辅导老师权限装饰器**
```python
@teacher_required
def some_function():
    # 只有 role='teacher' 可以访问
    pass
```

**定义位置**: `routes/teacher.py` - `teacher_required()`

### 5.2 数据隔离规则

#### **班主任数据隔离**

**老师数据**:
```python
# 只能看到自己创建的辅导老师
User.query.filter(
    User.role == 'teacher',
    User.created_by_user_id == current_user.id
)
```

**客户数据**:
```python
# 只能看到自己负责的客户
Customer.query.filter(
    Customer.teacher_user_id == current_user.id
)
```

#### **辅导老师数据隔离**

**学生数据**:
```python
# 只能看到分配给自己的学生
Customer.query.filter(
    Customer.teacher_id == current_user.teacher_profile.user_id
)
```

**权限检查示例**:
```python
# 检查学生是否属于当前老师
teacher = current_user.teacher_profile
if student.teacher_id != teacher.user_id:
    return error('您无权访问此学生信息')
```

---

## 📊 六、关键API接口

### 6.1 班主任相关API

| 接口路径 | 方法 | 功能 | 权限 |
|---------|------|------|------|
| `/teachers/list` | GET | 辅导老师列表 | teacher_supervisor |
| `/teachers/add` | GET/POST | 添加辅导老师 | teacher_supervisor |
| `/teachers/edit/<id>` | GET/POST | 编辑辅导老师 | teacher_supervisor |
| `/teachers/detail/<id>` | GET | 辅导老师详情 | teacher_supervisor |
| `/teachers/toggle_status/<id>` | POST | 启用/禁用老师 | teacher_supervisor |
| `/teachers/get_active_teachers` | GET | 获取启用老师列表(下拉) | teacher_supervisor |
| `/teachers/assign/<customer_id>` | POST | 分配辅导老师 | teacher_supervisor |
| `/teachers/change/<customer_id>` | POST | 更换辅导老师 | teacher_supervisor |

### 6.2 辅导老师相关API

| 接口路径 | 方法 | 功能 | 权限 |
|---------|------|------|------|
| `/teacher/students` | GET | 学生列表 | teacher |
| `/teacher/students/<id>` | GET | 学生详情 | teacher |
| `/teacher/students/<id>/api` | GET | 学生信息API | teacher |
| `/customers/<id>/update_progress` | POST | 更新课程进度 | teacher |
| `/customers/<id>/add_competition` | POST | 添加赛事 | teacher |
| `/customers/competitions/<id>/update` | POST | 更新赛事状态 | teacher |
| `/customers/competitions/<id>/delete` | DELETE | 删除赛事 | teacher |

---

## ⚠️ 七、当前系统缺失功能

### 7.1 缺少老师分配时间记录

**问题**:
- Customer 表没有 `teacher_assigned_at` 字段
- 无法追溯老师何时被分配给学生
- 更换老师后,无法知道原老师的分配时间

**影响**:
- 无法统计老师负责学生的时长
- 无法分析老师分配历史
- 数据分析受限

**建议方案**:
1. **简单方案**: 添加 `teacher_assigned_at` 字段
2. **完整方案**: 创建 `TeacherAssignment` 历史表

### 7.2 缺少老师分配历史记录

**问题**:
- 更换老师后,无法查看历史分配记录
- 无法追溯学生曾经由哪些老师负责过

**建议**: 创建老师分配历史表,记录每次分配/更换操作

---

## 📈 八、数据流转图

### 8.1 学生从线索到分配老师的完整流程

```
销售员创建线索
    ↓
Lead 表 (sales_user_id=销售ID)
    ↓
销售员/销售管理转化为客户
    ↓
Customer 表 (teacher_user_id=班主任ID, teacher_id=NULL)
    ↓
班主任分配辅导老师
    ↓
Customer 表 (teacher_user_id=班主任ID, teacher_id=辅导老师user_id)
    ↓
辅导老师管理学生
    ↓
TutoringDelivery (课程进度)
CustomerCompetition (赛事进展)
DeliveryDocument (交付文档)
```

### 8.2 老师创建和管理流程

```
管理员创建班主任
    ↓
User 表 (role=teacher_supervisor)
    ↓
班主任创建辅导老师
    ↓
User 表 (role=teacher, created_by_user_id=班主任ID)
    +
Teacher 表 (user_id=User.id, 专业信息)
    ↓
班主任分配老师给学生
    ↓
Customer.teacher_id = Teacher.user_id
```

---

## 🎯 九、核心业务规则总结

### 9.1 创建规则
1. ✅ 管理员创建班主任
2. ✅ 班主任创建辅导老师 (记录 `created_by_user_id`)
3. ❌ 辅导老师不能创建其他用户

### 9.2 分配规则
1. ✅ 销售转化客户时分配班主任
2. ✅ 班主任分配辅导老师给客户
3. ✅ 班主任只能分配自己创建的辅导老师
4. ✅ 禁用的老师不能被分配

### 9.3 数据访问规则
1. ✅ 班主任只能看到自己创建的辅导老师
2. ✅ 班主任只能看到自己负责的客户
3. ✅ 辅导老师只能看到分配给自己的学生
4. ✅ 销售管理可以查看所有辅导老师(只读)

### 9.4 操作权限规则
1. ✅ 班主任可以编辑自己创建的辅导老师信息
2. ✅ 班主任可以禁用/启用自己创建的辅导老师
3. ✅ 班主任可以更换客户的辅导老师
4. ✅ 辅导老师可以更新自己学生的课程进度和赛事进展
5. ✅ 辅导老师可以上传自己学生的交付文档

---

## 📝 十、相关文件清单

### 10.1 后端路由文件
- `routes/teachers.py` - 班主任管理辅导老师
- `routes/teacher.py` - 辅导老师功能
- `routes/delivery.py` - 交付管理(包含老师创建)
- `routes/customers.py` - 客户管理(包含进度更新)
- `routes/admin.py` - 管理员功能(创建班主任)

### 10.2 前端模板文件
- `templates/teachers/` - 班主任管理老师页面
  - `list.html` - 老师列表
  - `add.html` - 添加老师
  - `edit.html` - 编辑老师
  - `detail.html` - 老师详情
  - `list_for_sales.html` - 销售管理查看老师
- `templates/teacher/` - 辅导老师功能页面
  - `student_list.html` - 学生列表
  - `student_detail.html` - 学生详情
- `templates/delivery/` - 交付管理页面
  - `teacher_list.html` - 老师列表
  - `create_teacher.html` - 创建老师
  - `edit_teacher.html` - 编辑老师
- `templates/customers/` - 客户管理页面
  - `list.html` - 客户列表(包含分配老师功能)
  - `edit.html` - 编辑客户(包含选择老师)

### 10.3 数据模型文件
- `models.py` - 数据模型定义
  - `User` - 用户账号表
  - `Teacher` - 辅导老师信息表
  - `Customer` - 客户表
  - `TutoringDelivery` - 课程交付表
  - `CustomerCompetition` - 客户赛事表
  - `DeliveryDocument` - 交付文档表
  - `TeacherImage` - 老师图片表

### 10.4 权限控制文件
- `utils/permissions.py` - 权限装饰器
- `routes/teachers.py` - `teacher_supervisor_required()`
- `routes/teacher.py` - `teacher_required()`

---

**文档生成完成** 🎉


