# 服务器执行脚本 - 简明指南

## 🎯 你的问题：生产环境Logo显示不出

**根本原因**：Nginx配置文件缺失静态文件路径映射

---

## ⚡ 快速修复（3步搞定）

### 第1步：SSH登录服务器

```bash
ssh root@47.100.238.50
```

### 第2步：拉取最新修复脚本

```bash
cd /root/crm
git pull origin bak
```

### 第3步：执行修复脚本

```bash
bash fix_nginx_logo.sh
```

**就这么简单！** 🎉

---

## 📋 详细说明

### 脚本会自动做什么？

1. ✅ 检查Nginx配置文件位置
2. ✅ 查找CRM相关配置
3. ✅ 检查静态文件配置是否存在
4. ✅ 修复文件权限（755目录，644文件）
5. ✅ 测试Nginx配置
6. ✅ 重载Nginx服务
7. ✅ 测试Logo访问

### 如果脚本提示需要手动配置

脚本会显示需要添加的配置内容，你只需：

```bash
# 1. 编辑Nginx配置文件（脚本会告诉你文件路径）
nano /etc/nginx/sites-enabled/sxylab.com

# 2. 在 server { } 块中添加以下内容：
location /static/ {
    alias /root/crm/static/;
    expires 7d;
    add_header Cache-Control "public";
    access_log off;
}

# 3. 保存退出（Ctrl+X, Y, Enter）

# 4. 测试并重载Nginx
nginx -t
systemctl reload nginx
```

---

## ✅ 验证修复成功

### 方法1：命令行测试

```bash
curl -I http://localhost/static/images/custom-logo.png
```

**成功标志**：看到 `HTTP/1.1 200 OK`

### 方法2：浏览器测试

访问：https://sxylab.com/static/images/custom-logo.png

**成功标志**：能看到logo图片

### 方法3：清除浏览器缓存

如果还是看不到，清除缓存并强制刷新：
- Windows: `Ctrl+F5`
- Mac: `Cmd+Shift+R`

---

## 🔧 如果遇到问题

### 问题1：git pull失败

```bash
# 解决方法：先提交或放弃本地修改
git stash
git pull origin bak
```

### 问题2：脚本执行失败

```bash
# 查看详细错误
bash -x fix_nginx_logo.sh

# 或查看Nginx错误日志
tail -f /var/log/nginx/error.log
```

### 问题3：权限不足

```bash
# 确保使用root权限
sudo bash fix_nginx_logo.sh
```

---

## 📚 更多帮助

- **详细修复指南**：查看 `Nginx配置Logo显示-快速修复指南.md`
- **脚本使用说明**：查看 `脚本使用说明.md`
- **Logo问题排查**：查看 `logo显示问题排查.md`

---

## 🎬 完整操作示例

```bash
# 1. 登录服务器
ssh root@47.100.238.50

# 2. 进入项目目录
cd /root/crm

# 3. 拉取最新代码
git pull origin bak

# 4. 执行修复脚本
bash fix_nginx_logo.sh

# 5. 测试访问
curl -I http://localhost/static/images/custom-logo.png

# 6. 如果返回200，在浏览器中访问
# https://sxylab.com/static/images/custom-logo.png

# 7. 清除浏览器缓存并刷新页面
```

---

## 💡 小贴士

1. **备份很重要**：脚本会自动备份Nginx配置
2. **测试先行**：修改后先用 `nginx -t` 测试
3. **查看日志**：遇到问题先看 `/var/log/nginx/error.log`
4. **清除缓存**：浏览器缓存可能导致看不到更新

---

## ✨ 预期结果

修复成功后：
- ✅ Logo在网站上正常显示
- ✅ Favicon（标签栏图标）正常显示
- ✅ 所有静态资源（CSS、JS、图片）都能访问
- ✅ 没有404或403错误

**祝你修复顺利！** 🚀

