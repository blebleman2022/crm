# 服务器更新快速指南

**更新日期**: 2026-02-04  
**预计耗时**: 5-10 分钟

---

## 🚀 快速操作步骤

### 1️⃣ 登录服务器
```bash
ssh -i login.pem user@your-server-ip
cd /path/to/crm
```

### 2️⃣ 备份数据库
```bash
mkdir -p backups
cp instance/edu_crm.db backups/edu_crm_backup_$(date +%Y%m%d_%H%M%S).db
```

### 3️⃣ 拉取代码
```bash
git checkout bak
git pull origin bak
```

### 4️⃣ 执行数据库迁移
```bash
chmod +x migrate_topic_tasks.py
python3 migrate_topic_tasks.py
```

**预期输出**: 看到 "🎉 数据库迁移成功完成!" 表示成功

### 5️⃣ 安装依赖(如果需要)
```bash
source venv/bin/activate  # 如果使用虚拟环境
pip3 install -r requirements.txt
```

### 6️⃣ 重启服务
```bash
sudo systemctl restart crm
```

### 7️⃣ 验证状态
```bash
sudo systemctl status crm
sudo journalctl -u crm -n 20 -f
```

按 `Ctrl+C` 退出日志查看

---

## ✅ 验证功能

### 测试班主任功能
1. 登录班主任账号
2. 访问线索列表 - 应该能正常访问
3. 查看线索详情 - 应该能看到头脑风暴字段

### 测试辅导老师功能
1. 登录辅导老师账号
2. 访问 `/teacher/topic-tasks` - 应该能看到任务列表

---

## 🆘 如果出现问题

### 迁移失败
```bash
# 查看错误信息,然后联系技术支持
python3 migrate_topic_tasks.py
```

### 服务启动失败
```bash
# 查看日志
sudo journalctl -u crm -n 50 --no-pager
```

### 紧急回滚
```bash
sudo systemctl stop crm
cp backups/edu_crm_backup_*.db instance/edu_crm.db
git reset --hard 37f1acf
sudo systemctl start crm
```

---

## 📞 技术支持

如有问题,请提供:
1. 错误日志截图
2. 执行的命令
3. 服务器环境信息

---

**详细文档**: 请查看 `服务器更新操作指南_2026-02-04.md`


